{
  "name": "github-mcp",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "type-check": "tsc --noEmit",
    "test": "vitest run --passWithNoTests",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "dependencies": {
    "@mcp-stack/auth-bearer": "workspace:*",
    "@mcp-stack/http-fetch": "workspace:*",
    "@mcp-stack/mcp-core": "workspace:*",
    "@modelcontextprotocol/sdk": "^1.29.0",
    "agents": "^0.11.6",
    "zod": "^3.24.0"
  }
}
